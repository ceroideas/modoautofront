import { Select2DataPipe } from './select2-data.pipe';

describe('Select2DataPipe', () => {
  it('create an instance', () => {
    const pipe = new Select2DataPipe();
    expect(pipe).toBeTruthy();
  });
});
