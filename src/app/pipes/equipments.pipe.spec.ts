import { EquipmentsPipe } from './equipments.pipe';

describe('EquipmentsPipe', () => {
  it('create an instance', () => {
    const pipe = new EquipmentsPipe();
    expect(pipe).toBeTruthy();
  });
});
