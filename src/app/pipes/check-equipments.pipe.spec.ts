import { CheckEquipmentsPipe } from './check-equipments.pipe';

describe('CheckEquipmentsPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckEquipmentsPipe();
    expect(pipe).toBeTruthy();
  });
});
